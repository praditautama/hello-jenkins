pipeline {
  agent any
  stages {
    stage('Test A') {
      steps {
        sh 'echo \'shell scripts to build project...\''
      }
    }
    stage('Test B') {
      steps {
        sh 'echo \'shell scripts to build project...\''
      }
    }
  }
}